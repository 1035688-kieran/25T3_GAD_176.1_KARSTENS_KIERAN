using UnityEngine;

public class ShadowToy : CollectableScript
{
    private bool shadowDead;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
